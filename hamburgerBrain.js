const hamburgerSentence = document.querySelector('.hamburgerSentence');
const hamburgerSentence2 = document.querySelector('.hamburgerSentence2');
const hamburgerSentence3 = document.querySelector('.hamburgerSentence3');
const hamburgerSentence4 = document.querySelector('.hamburgerSentence4');
const hamburgerSentence5 = document.querySelector('.hamburgerSentence5');
const hamburgerSentence6 = document.querySelector('.hamburgerSentence6');
const hamburgerSentence7 = document.querySelector('.hamburgerSentence7');
const hamburgerSentence8 = document.querySelector('.hamburgerSentence8');
const hamburgerSentence9 = document.querySelector('.hamburgerSentence9');
const hamburgerSentence10 = document.querySelector('.hamburgerSentence10');
const hamburgerSentence11 = document.querySelector('.hamburgerSentence11');
const hamburgerSentence12 = document.querySelector('.hamburgerSentence12');
const hamburgerSentence13 = document.querySelector('.hamburgerSentence13');
const hamburgerSentence14 = document.querySelector('.hamburgerSentence14');
const hamburgerSentence15 = document.querySelector('.hamburgerSentence15');


let degree = 0;
let degree2 = 0;
let degree3 = 0;
let degree4 = 0;
let degree5 = 0;
let degree6 = 0;
let degree7 = 0;
let degree8 = 0;
let degree9 = 0;
let degree10 = 0;
let degree11 = 0;
let degree12 = 0;
let degree13 = 0;
let degree14 = 0;
let degree15 = 0;
loop();

function loop() {
    const rotation = (degree * Math.PI) / 180;
    const rotation2 = (degree2 * Math.PI) / 180;
    const rotation3 = (degree3 * Math.PI) / 180;
    const rotation4 = (degree4 * Math.PI) / 180;
    const rotation5 = (degree5 * Math.PI) / 180;
    const rotation6 = (degree6 * Math.PI) / 180;
    const rotation7 = (degree7 * Math.PI) / 180;
    const rotation8 = (degree8 * Math.PI) / 180;
    const rotation9 = (degree9 * Math.PI) / 180;
    const rotation10 = (degree10 * Math.PI) / 180;
    const rotation11 = (degree11 * Math.PI) / 180;
    const rotation12 = (degree12 * Math.PI) / 180;
    const rotation13 = (degree13 * Math.PI) / 180;
    const rotation14 = (degree14 * Math.PI) / 180;
    const rotation15 = (degree15 * Math.PI) / 180;
    

    const targetX = window.innerWidth / 2 + 100 * Math.cos(rotation) - 100;
    const targetY = window.innerHeight / 2 + 100 * Math.sin(rotation) - 500;
    const targetX2 = window.innerWidth / 2 + 100 * Math.cos(rotation2) - 80;
    const targetY2 = window.innerHeight / 2 + 100 * Math.sin(rotation2) - 300;
    const targetX3 = window.innerWidth / 2 + 100 * Math.cos(rotation3) - 100;
    const targetY3 = window.innerHeight / 2 + 100 * Math.sin(rotation3) - 100;
    const targetX4 = window.innerWidth / 2 + 100 * Math.cos(rotation4) - 120;
    const targetY4 = window.innerHeight / 2 + 100 * Math.sin(rotation4) + 100;
    const targetX5 = window.innerWidth / 2 + 100 * Math.cos(rotation5) - 100;
    const targetY5 = window.innerHeight / 2 + 100 * Math.sin(rotation5) + 300;
    const targetX6 = window.innerWidth / 2 + 100 * Math.cos(rotation6) - 420;
    const targetY6 = window.innerHeight / 2 + 100 * Math.sin(rotation6) - 500;
    const targetX7 = window.innerWidth / 2 + 100 * Math.cos(rotation7) - 440;
    const targetY7 = window.innerHeight / 2 + 100 * Math.sin(rotation7) - 300;
    const targetX8 = window.innerWidth / 2 + 100 * Math.cos(rotation8) - 460;
    const targetY8 = window.innerHeight / 2 + 100 * Math.sin(rotation8) - 100;
    const targetX9 = window.innerWidth / 2 + 100 * Math.cos(rotation9) - 440;
    const targetY9 = window.innerHeight / 2 + 100 * Math.sin(rotation9) + 100;
    const targetX10 = window.innerWidth / 2 + 100 * Math.cos(rotation10) - 440;
    const targetY10 = window.innerHeight / 2 + 100 * Math.sin(rotation10) + 320;
    const targetX11 = window.innerWidth / 2 + 100 * Math.cos(rotation11) + 240;
    const targetY11 = window.innerHeight / 2 + 100 * Math.sin(rotation11) - 500;
    const targetX12 = window.innerWidth / 2 + 100 * Math.cos(rotation12) + 220;
    const targetY12 = window.innerHeight / 2 + 100 * Math.sin(rotation12) - 300;
    const targetX13 = window.innerWidth / 2 + 100 * Math.cos(rotation13) + 240;
    const targetY13 = window.innerHeight / 2 + 100 * Math.sin(rotation13) - 100;
    const targetX14 = window.innerWidth / 2 + 100 * Math.cos(rotation14) + 280;
    const targetY14 = window.innerHeight / 2 + 100 * Math.sin(rotation14) + 100;
    const targetX15 = window.innerWidth / 2 + 100 * Math.cos(rotation15) + 240;
    const targetY15 = window.innerHeight / 2 + 100 * Math.sin(rotation15) + 300;

    hamburgerSentence.style.left = `${targetX}px`;
    hamburgerSentence.style.top = `${targetY}px`;
    hamburgerSentence2.style.left = `${targetX2}px`;
    hamburgerSentence2.style.top = `${targetY2}px`;
    hamburgerSentence3.style.left = `${targetX3}px`;
    hamburgerSentence3.style.top = `${targetY3}px`;
    hamburgerSentence4.style.left = `${targetX4}px`;
    hamburgerSentence4.style.top = `${targetY4}px`;
    hamburgerSentence5.style.left = `${targetX5}px`;
    hamburgerSentence5.style.top = `${targetY5}px`;
    hamburgerSentence6.style.left = `${targetX6}px`;
    hamburgerSentence6.style.top = `${targetY6}px`;
    hamburgerSentence7.style.left = `${targetX7}px`;
    hamburgerSentence7.style.top = `${targetY7}px`;
    hamburgerSentence8.style.left = `${targetX8}px`;
    hamburgerSentence8.style.top = `${targetY8}px`;
    hamburgerSentence9.style.left = `${targetX9}px`;
    hamburgerSentence9.style.top = `${targetY9}px`;
    hamburgerSentence10.style.left = `${targetX10}px`;
    hamburgerSentence10.style.top = `${targetY10}px`;
    hamburgerSentence11.style.left = `${targetX11}px`;
    hamburgerSentence11.style.top = `${targetY11}px`;
    hamburgerSentence12.style.left = `${targetX12}px`;
    hamburgerSentence12.style.top = `${targetY12}px`;
    hamburgerSentence13.style.left = `${targetX13}px`;
    hamburgerSentence13.style.top = `${targetY13}px`;
    hamburgerSentence14.style.left = `${targetX14}px`;
    hamburgerSentence14.style.top = `${targetY14}px`;
    hamburgerSentence15.style.left = `${targetX15}px`;
    hamburgerSentence15.style.top = `${targetY15}px`;
    
    degree += 1 + Math.floor(Math.random() * 5);
    degree2 += 1 + Math.floor(Math.random() * 5);
    degree3 += 1 + Math.floor(Math.random() * 5);
    degree4 += 1 + Math.floor(Math.random() * 5);
    degree5 += 1 + Math.floor(Math.random() * 5);
    degree6 += 1 + Math.floor(Math.random() * 5);
    degree7 += 1 + Math.floor(Math.random() * 5);
    degree8 += 1 + Math.floor(Math.random() * 5);
    degree9 += 1 + Math.floor(Math.random() * 5);
    degree10 += 1 + Math.floor(Math.random() * 5);
    degree11 += 1 + Math.floor(Math.random() * 5);
    degree12 += 1 + Math.floor(Math.random() * 5);
    degree13 += 1 + Math.floor(Math.random() * 5);
    degree14 += 1 + Math.floor(Math.random() * 5);
    degree15 += 1 + Math.floor(Math.random() * 5);
    
    requestAnimationFrame(loop);
}